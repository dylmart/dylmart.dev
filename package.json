{
  "name": "dylmart.dev",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "test": "vitest run && astro build && playwright test",
    "test:unit": "vitest run",
    "deploy": "bash scripts/deploy-home.sh",
    "compile:sims": "node scripts/compile-sims.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@fontsource/archivo-black": "^5.2.8",
    "@fontsource/space-grotesk": "^5.2.10",
    "@fontsource/space-mono": "^5.2.9",
    "astro": "^7.0.6",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "vitest": "^4.1.10"
  }
}
